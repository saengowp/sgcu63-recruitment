The `fileSearch` function is inside the `script.py`.

Usage
--------------------

Use python3 to import the `script.py`. No additional library needed.

```
>>> from script import fileSearch
>>> fileSearch("file", '{"_files":["file", "fileA"], "dirA":{}}')
```

